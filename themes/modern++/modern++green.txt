# === Instructions ===
# 1. Copy this code.
# 2. Paste it into your terminal to preview the prompt.
# 3. To keep it permanent, append it to your ~/.bashrc file.
# 4. Reload the shell: source ~/.bashrc

PS1='\[\e[38;5;2m\]\[\e[48;5;2m\]\[\e[38;5;15m\] $(name="$(getent passwd "$USER" | cut -d: -f5 | cut -d, -f1)"; if [ -z "$name" ]; then printf "%s" "$USER"; else printf "%s" "$name"; fi) \W \[\e[0m\]\[\e[38;5;2m\]\[\e[0m\] ❯ '
