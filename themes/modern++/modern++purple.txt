# === Instructions ===
# 1. Copy this snippet.
# 2. Paste it into your terminal to see the new prompt.
# 3. Add it to ~/.bashrc to save it permanently.
# 4. Reload Bash with: source ~/.bashrc

PS1='\[\e[38;5;5m\]\[\e[48;5;5m\]\[\e[38;5;15m\] $(name="$(getent passwd "$USER" | cut -d: -f5 | cut -d, -f1)"; if [ -z "$name" ]; then printf "%s" "$USER"; else printf "%s" "$name"; fi) \W \[\e[0m\]\[\e[38;5;5m\]\[\e[0m\] ❯ '
